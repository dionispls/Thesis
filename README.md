spec/ — фиксируем определения метрик и методику

host/ — Django + код инвокеров (WASM и Docker)

artifacts/ — тут появятся .wasm и Dockerfile/образ

results/ — сюда будем складывать JSON с результатами запусков